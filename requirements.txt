# PRIME OS — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Pydantic v2 (used by FastAPI)
pydantic>=2.7.0
pydantic-settings>=2.3.0

# LiteLLM — model-agnostic LLM abstraction (Claude, OpenAI, Ollama, vLLM, …)
litellm>=1.40.0

# Knowledge graph
networkx>=3.3

# YAML parsing (decision frontmatter)
PyYAML>=6.0

# HTTP client (agent web_search tool)
httpx>=0.27.0

# PostgreSQL — async SQLAlchemy + asyncpg driver
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.0   # sync driver used by Alembic migrations at startup

# Dev / tooling
python-dotenv>=1.0.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
anyio[trio]>=4.0.0
